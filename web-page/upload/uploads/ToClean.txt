void Server::ReadClientResponse(Client &client)
void Server::ProcessRequest(Client& client, int redirectCount = 0)


check Your server must never block and the client can be bounced properly if necessary.



capiamo questo 		while ((bytesRead = read(pipefd[0], buffer, sizeof(buffer))) > 0)
			output.append(buffer, bytesRead);